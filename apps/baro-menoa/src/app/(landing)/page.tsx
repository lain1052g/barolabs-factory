// This route group page is intentionally unused.
// The root route (/) is handled by app/page.tsx.
// Kept here to avoid import errors from (landing)/components.
export { default } from '@/app/page';
